## 怪异模式下的怪癖行为

浏览器的渲染模式：怪异模式、标准模式，兼容老版本浏览器，如低版本IE（用的怪异模式）

1. width 会包含 padding 和 border（IE 盒模型），而标准模式下 width 只算内容区
2. 在表格中的字体样式不会继承外部字体设置
3. 怪异模式下可以设置行内元素宽高
4. 怪异模式下white-space:pre会失效

- HTML5 时代，只要在文档第一行写 <!DOCTYPE html> 就会进入标准模式，这是现在唯一推荐的做法。
- 如果忘了写 DOCTYPE，浏览器会自动进入怪异模式，可能导致样式和布局与你预期不一致，排查起来很费劲。