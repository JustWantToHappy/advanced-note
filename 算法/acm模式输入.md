# javascript
```javascript
const fs = require("fs")
const input = fs.readFileSync(0, 'utf-8').trim()

const data = input.split(/\s+/)
```
# python3

```python
 n, m = map(int, input().split())
```